/*
숫자 두개를 매개변수로 받아 곱을 리턴하는 함수를 만들고 싶다.
*/
export const forModule = 4;

function multiply(x: number, y: number) {
  return x * y;
}

let numbers = multiply(3, 6);
//const strings = multiply('a', 'b');
console.log(numbers);
//console.log(strings);
